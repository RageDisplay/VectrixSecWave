# Python dependencies for VectrixSecWave (pip / venv install).
# On Kali you can instead use system packages — see requirements-kali.txt.
#
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# External CLI tools (nikto, nuclei, gobuster, sqlmap, whatweb, wafw00f,
# sslscan) are NOT pip packages — install them via your distro / Kali.

requests>=2.28
beautifulsoup4>=4.11
urllib3>=1.26
